# URL da API backend (rota /admin/* e /:slug)
VITE_API_URL=http://localhost:3000

# URL pública dos links curtos (usado pra montar a URL completa no painel)
# Tipicamente igual à VITE_API_URL em dev, e o domínio público em produção.
VITE_BASE_URL=http://localhost:3000
